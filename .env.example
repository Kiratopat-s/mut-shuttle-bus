# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# PostgreSQL Connection with Pool Settings
# connection_limit: Maximum number of connections in the pool (default: num_cpus * 2 + 1)
# pool_timeout: Maximum time to wait for a connection from the pool (in seconds)
# connect_timeout: Maximum time to wait when connecting to the database (in seconds)
DATABASE_URL="postgresql://admin:password@hotline52.thddns.net:8181/shuttle_bus?connection_limit=5&pool_timeout=10&connect_timeout=10"

# JWT Secret for authentication
JWT_SECRET="shuttlebussecret"